@model IEnumerable<Roostersysteem.Models.UrenBeschikbaarheid>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TijdstipBeschikbaarheid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DagBeschikbaarheid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TijdstipBeschikbaarheid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DagBeschikbaarheid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UrenBeschikbaarheidId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UrenBeschikbaarheidId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UrenBeschikbaarheidId })
        </td>
    </tr>
}

</table>
